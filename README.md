# week-14-makeup


1) Generate SSH key with ssh-keygen -t rsa -b 4096 -f ~/.ssh/aws


2) Run the `import-lab-key` script

3) cd to terrafor folder

4) terraform init

5) terraform fmt

6) terraform plan

7) terraform apply

8) cd to ansible folder

9) ansible-playbook playbook.yml



![database](database.png)
![web](web.png)
